// string
const stageName: string = 'A Beautiful Vue';

// number
const roomSize: number = 100;

// boolean
let isCompleted: boolean = false;

// array
const shoppingList: string[] = ['apple', 'bananas', 'sherries'];

// function
const generateFullName = (firstName: string, lastName: string): string => {
  return firstName + '' + lastName
}

// object
let person: {
  name: string;
  age: number;
  activeAvenger: boolean;
  powers: string[];
} = {
  name: 'Peter Parker',
  age: 20,
  activeAvenger: true,
  powers: ['wall-crawl', 'spider-sense']
}

// interface